import { View, Text } from 'react-native'
import React from 'react'

const menuItems = [
  { title: 'Logout' },
];

export default function Profile() {
  return (
    <View>
      <Text>Profile</Text>
    </View>
  )
}